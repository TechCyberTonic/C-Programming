#include <stdio.h>
#include <stdbool.h>
int main()
{
  bool iscomputer = "True";
  printf("hello world!");
  if(iscomputer == true)
  {
    printf("Setup completed in Computer");
  }
  else
  {
    printf("Setup recommended!");
  }
  return 0;
}
